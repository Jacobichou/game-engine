package entities;

import level.Level;

public class GreenWave extends Mascot {

	public GreenWave(Level level, int x, int y,
			int speed) {
		super(level, "Green Wave", "Tulane", x, y, speed);
	}

}
